###############################################################################
# 코어 수치/데이터 (기본 토대) — 상위 패키지가 하위에 의존하므로 이 순서 권장
###############################################################################
numpy                # 수치 계산(최우선, 다수 패키지의 기반)
pandas               # 데이터프레임; 내부적으로 numpy/pyarrow 등과 연계
# pyarrow              # Parquet/Arrow I/O (pandas와 함께 사용 빈번)

# scipy              # 과학연산(최적화/통계 등). 용량/빌드 부담 ↑ → 필요 시 주석 해제

###############################################################################
# 파일 I/O / 포맷
###############################################################################
openpyxl             # 최신 Excel(xlsx) 입출력
# xlrd               # 구형 xls 읽기(요즘은 거의 불필요)
# fastparquet        # Parquet 대안(보통 pyarrow로 충분)

###############################################################################
# 시각화 & EDA (matplotlib → seaborn 순서 / 의존 고려)
###############################################################################
matplotlib           # 기본 시각화(다른 라이브러리의 기반)
seaborn              # 통계 시각화(내부적으로 matplotlib 사용)
plotly               # 대화형 시각화(브라우저 렌더)
# altair               # 선언적 시각화(간결한 문법)
# missingno            # 결측치 시각화(EDA)
# ydata-profiling    # (구 pandas-profiling) 자동 EDA 리포트 — 의존도·리소스 ↑

###############################################################################
# 네트워킹 & API (경량 / 범용)
#  ※ Piston 컨테이너는 기본 네트워크 차단(PISTON_DISABLE_NETWORKING=true) 설정일 수 있음.
#    외부통신이 필요없다면 그대로 유지. 필요하면 컨테이너 정책을 별도 관리하세요.
###############################################################################
requests             # 동기 HTTP 클라이언트(표준처럼 쓰임)
# httpx                # requests 대체(동기/비동기 모두 지원)
# aiohttp            # 순수 비동기 HTTP가 꼭 필요할 때

###############################################################################
# 직렬화 & 데이터 교환
###############################################################################
# pyyaml               # YAML 입출력
# orjson               # 매우 빠른 JSON 직렬화(휠 제공; 성능 우수)
# ujson              # 대안 JSON 라이브러리

###############################################################################
# 유틸리티 / 로깅 / CLI
###############################################################################
# tqdm                 # 진행률 표시
# joblib               # 캐싱/병렬 실행(간단 병렬 처리에 유용)
# python-dotenv        # .env 파일 로드(환경변수 관리)
# loguru               # 편리한 로깅
# pydantic             # 데이터 모델/검증(v1/v2 API 차이 유의)
# dateparser           # 자연어 날짜 파싱(“내일”, “+3 days” 등)
# click                # CLI 프레임워크(typer의 기반)
# typer                # 간결한 CLI 생성(내부적으로 click 사용)
# fire               # 구글 Fire; 초간단 CLI — 선택적

###############################################################################
# 콘솔 출력/표 렌더 (범용)
###############################################################################
# rich                 # 컬러 로그/트레이스/테이블 등 터미널 렌더링
# tabulate             # 표 형태 출력(간단)
# prettytable          # ASCII 테이블 출력(대안)
# colorama             # 콘솔 컬러(윈도우 호환성 등)

###############################################################################
# 이미지/문서 처리 (상대적으로 용량 ↑ 가능성 / 선택)
###############################################################################
pillow               # 이미지 처리(PIL 포크; 비교적 경량)
# opencv-python      # 매우 강력하지만 용량/빌드 부담 큼 → 필요 시만
# pdfplumber         # PDF 텍스트 추출(의존 다수)
# reportlab          # PDF 생성
# python-docx        # Word 파일 I/O

###############################################################################
# 머신러닝/모델링 (선택 — 리소스·빌드 부담 ↑)
###############################################################################
scikit-learn         # 전통 ML & 전처리(상당히 유용, 용량 보통)
# xgboost            # 부스팅 모델(휠 제공되지만 용량/빌드 부담 有)
# lightgbm           # 빠른 부스팅(환경 따라 빌드 이슈 가능)
# catboost           # 범주형 데이터에 강함(용량 ↑)

###############################################################################
# 딥러닝 (필요할 때만! 이미지 무거워짐)
###############################################################################
# torch              # PyTorch
# torchvision        # PyTorch 비전
# torchaudio         # PyTorch 오디오
# tensorflow         # TensorFlow
# transformers       # HF 트랜스포머 (NLP 중심, 모델 다운로드 필요)

###############################################################################
# 유틸리티 & 기타 (build.sh에서 이동)
###############################################################################
# pycrypto             # 암호화 라이브러리 (구형, 가능하면 cryptography 권장)
# whoosh               # 텍스트 인덱싱 및 검색
# bcrypt               # 패스워드 해싱
# passlib              # 패스워드 해싱 (다양한 알고리즘 지원)
sympy                # 기호 수학 (심볼릭 연산)
xxhash               # 빠른 해시 알고리즘
base58               # Base58 인코딩 (비트코인 주소 등에 사용)
cryptography         # 최신 암호화 라이브러리 (pycrypto 대체)
PyNaCl               # Networking and Cryptography library